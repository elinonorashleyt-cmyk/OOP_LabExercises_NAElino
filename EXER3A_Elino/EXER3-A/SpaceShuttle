public class Airplane extends AirTransport {
    public Airplane(int capacity, int maxAltitude) {
        super("Airplane", capacity, maxAltitude);
    }
}

public class SpaceShuttle extends AirTransport {
    public SpaceShuttle(int capacity, int maxAltitude) {
        super("Space Shuttle", capacity, maxAltitude);
    }
}